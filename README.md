Password Checker (Python)

This is a simple command-line password checker written in Python.

The program asks the user to enter a password and checks whether it matches a predefined correct password.  
The user can keep trying until the correct password is entered or choose to quit.
